# 🤖 คู่มือการใช้งาน AI Agent System

## 📋 สารบัญ

1. [ภาพรวม](#ภาพรวม)
2. [การเข้าสู่ระบบ Admin](#การเข้าสู่ระบบ-admin)
3. [การเพิ่มเว็บไซต์](#การเพิ่มเว็บไซต์)
4. [การ Trigger Scraping](#การ-trigger-scraping)
5. [การดูผลลัพธ์](#การดูผลลัพธ์)
6. [การจัดการเว็บไซต์](#การจัดการเว็บไซต์)
7. [FAQ](#faq)

---

## 🎯 ภาพรวม

**AI Agent System** เป็นระบบที่ใช้ AI ช่วยเก็บข้อมูลต้นไม้และราคาจากเว็บไซต์อัตโนมัติ โดย:

1. **Scrape** เว็บไซต์ (ใช้ Puppeteer/Cheerio)
2. **Extract** ข้อมูลด้วย AI (GPT-4o)
3. **Validate & Clean** ข้อมูลด้วย AI
4. **Save** ข้อมูลลงฐานข้อมูลอัตโนมัติ

### ✨ คุณสมบัติ

- ✅ ใช้ AI แกะข้อมูลจากเว็บไซต์อัตโนมัติ
- ✅ ใช้ AI จัดระเบียบและตรวจสอบข้อมูล
- ✅ ใช้ AI จับคู่ข้อมูลกับฐานข้อมูล
- ✅ บันทึกข้อมูลลงฐานข้อมูลอัตโนมัติ
- ✅ ติดตามประวัติการ scrape

---

## 🔐 การเข้าสู่ระบบ Admin

### ขั้นตอน

1. **ไปที่หน้า Login**
   - คลิก "Admin" หรือ "Admin Login" ในเมนู
   - หรือไปที่ `/admin-login`

2. **ใส่ Password**
   - Password: `1234` (ตั้งค่าใน Railway: `ADMIN_PASSWORD=1234`)

3. **Login สำเร็จ**
   - ระบบจะ redirect ไปหน้า `/ai-agent` อัตโนมัติ
   - จะเห็นลิงก์ "AI Agent" ในเมนู

---

## 🌐 การเพิ่มเว็บไซต์

### ขั้นตอน

1. **เข้าหน้า AI Agent**
   - ไปที่ `/ai-agent` (ต้อง login แล้ว)
   - หรือคลิก "AI Agent" ในเมนู

2. **กดปุ่ม "เพิ่มเว็บไซต์"**
   - อยู่ที่ด้านบนขวาของหน้า "เว็บไซต์"

3. **กรอกข้อมูลเว็บไซต์**
   - **ชื่อเว็บไซต์**: เช่น "ร้านต้นไม้ ABC"
   - **URL**: เช่น `https://example.com`
   - **คำอธิบาย** (ไม่บังคับ): เช่น "ร้านต้นไม้ในกรุงเทพ"
   - **Schedule**: 
     - `manual` - Trigger เอง (แนะนำสำหรับเริ่มต้น)
     - `daily` - Scrape ทุกวัน
     - `weekly` - Scrape ทุกสัปดาห์

4. **กด "เพิ่มเว็บไซต์"**
   - ระบบจะบันทึกเว็บไซต์ลงฐานข้อมูล

### ตัวอย่าง

```
ชื่อเว็บไซต์: ร้านต้นไม้ ABC
URL: https://www.plantstore.com/products
คำอธิบาย: ร้านต้นไม้ในกรุงเทพ มีต้นไม้หลากหลายชนิด
Schedule: manual
```

---

## 🔄 การ Trigger Scraping

### ขั้นตอน

1. **ไปที่หน้า "เว็บไซต์"**
   - จะเห็นรายการเว็บไซต์ทั้งหมด

2. **กดปุ่ม "Scrape"**
   - อยู่ที่เว็บไซต์ที่ต้องการ scrape

3. **รอผลลัพธ์**
   - ระบบจะเริ่ม scrape และใช้ AI แกะข้อมูล
   - ใช้เวลาประมาณ 30-60 วินาที (ขึ้นอยู่กับขนาดเว็บไซต์)

4. **ตรวจสอบผลลัพธ์**
   - ไปที่แท็บ "Jobs" เพื่อดูสถานะการ scrape
   - ไปที่แท็บ "Results" เพื่อดูข้อมูลที่ scrape ได้

### สถานะการ Scrape

- **Pending** ⏳ - กำลังรอ
- **Processing** 🔄 - กำลัง scrape และใช้ AI แกะข้อมูล
- **Completed** ✅ - สำเร็จ
- **Failed** ❌ - เกิดข้อผิดพลาด

---

## 📊 การดูผลลัพธ์

### แท็บ "Jobs"

แสดงประวัติการ scrape ทั้งหมด:

- **Status**: สถานะการ scrape (pending, processing, completed, failed)
- **URL**: URL ที่ scrape
- **Started**: เวลาที่เริ่ม scrape
- **Completed**: เวลาที่เสร็จสิ้น

### แท็บ "Results"

แสดงผลลัพธ์การ scrape:

- **Plant Name**: ชื่อต้นไม้ที่ scrape ได้
- **Price**: ราคาที่ scrape ได้
- **Size**: ขนาด (ถ้ามี)
- **Confidence**: ความมั่นใจในการแกะข้อมูล (0-100%)

### ตัวอย่างผลลัพธ์

```
Plant: มอนสเตอร่า
Price: ฿450
Size: 1-2 ฟุต
Confidence: 95%
```

---

## ⚙️ การจัดการเว็บไซต์

### แก้ไขเว็บไซต์

- ไปที่หน้า "เว็บไซต์"
- คลิกปุ่ม "Edit" (ถ้ามี)

### ลบเว็บไซต์

1. ไปที่หน้า "เว็บไซต์"
2. คลิกปุ่ม "ลบ" (🗑️) ที่เว็บไซต์ที่ต้องการลบ
3. ยืนยันการลบ

---

## 🔍 ระบบทำงานอย่างไร

### 1. Scrape HTML
```
เว็บไซต์ → Puppeteer/Cheerio → HTML Content
```

### 2. Extract Text
```
HTML Content → Extract Text → Text Content
```

### 3. AI Analysis
```
Text Content → GPT-4o → Structured Data
```

### 4. AI Validation
```
Structured Data → GPT-4o → Validated & Cleaned Data
```

### 5. Save to Database
```
Validated Data → Database → Plants & Suppliers
```

---

## 💡 Tips & Best Practices

### 1. เลือกเว็บไซต์ที่เหมาะสม
- ✅ ใช้เว็บไซต์ที่มีข้อมูลต้นไม้และราคาชัดเจน
- ✅ หลีกเลี่ยงเว็บไซต์ที่ต้อง login
- ✅ หลีกเลี่ยงเว็บไซต์ที่มี JavaScript ซับซ้อนมาก

### 2. ตรวจสอบผลลัพธ์
- ตรวจสอบผลลัพธ์ในแท็บ "Results" ทุกครั้ง
- ดู Confidence score - ถ้าต่ำกว่า 80% อาจต้อง scrape ใหม่
- ตรวจสอบว่าข้อมูลถูกบันทึกลงฐานข้อมูลหรือไม่

### 3. จัดการ Schedule
- เริ่มต้นใช้ `manual` เพื่อทดสอบก่อน
- หลังจากทดสอบแล้ว เปลี่ยนเป็น `daily` หรือ `weekly` ตามต้องการ

### 4. ตรวจสอบ Error
- ถ้า scrape fail ให้ดู error message ในแท็บ "Jobs"
- อาจเป็นเพราะ:
  - URL ไม่ถูกต้อง
  - เว็บไซต์มี JavaScript ซับซ้อน
  - เว็บไซต์บล็อก scraping
  - AI ไม่สามารถแกะข้อมูลได้

---

## ❓ FAQ

### Q: ต้องใช้ API Key ไหม?
**A:** ใช่ ต้องตั้งค่า `OPENAI_API_KEY` ใน Railway

### Q: Scrape ใช้เวลานานแค่ไหน?
**A:** ประมาณ 30-60 วินาทีต่อเว็บไซต์ (ขึ้นอยู่กับขนาดเว็บไซต์)

### Q: ข้อมูลถูกบันทึกลงฐานข้อมูลอัตโนมัติไหม?
**A:** ใช่ ระบบจะบันทึกข้อมูลต้นไม้และร้านค้าลงฐานข้อมูลอัตโนมัติ

### Q: สามารถ scrape เว็บไซต์ที่ต้อง login ได้ไหม?
**A:** ไม่ได้ ระบบจะ scrape เฉพาะข้อมูลสาธารณะ

### Q: ถ้า scrape fail ต้องทำยังไง?
**A:** 
1. ตรวจสอบ URL ว่าถูกต้องหรือไม่
2. ลอง scrape ใหม่
3. ตรวจสอบ error message ในแท็บ "Jobs"

### Q: ข้อมูลซ้ำซ้อนจะถูกจัดการอย่างไร?
**A:** ระบบจะ:
- จับคู่ต้นไม้ที่มีชื่อเดียวกัน
- จับคู่งานที่มีชื่อเดียวกัน
- อัพเดทราคาใหม่แทนการสร้างข้อมูลซ้ำ

---

## 🚀 ตัวอย่างการใช้งาน

### ตัวอย่างที่ 1: เพิ่มเว็บไซต์แรก

1. Login เข้าระบบ Admin
2. ไปที่หน้า "AI Agent"
3. กด "เพิ่มเว็บไซต์"
4. กรอก:
   - ชื่อ: "ร้านต้นไม้ ABC"
   - URL: `https://plantstore.com/products`
   - Schedule: `manual`
5. กด "เพิ่มเว็บไซต์"
6. กด "Scrape" ที่เว็บไซต์ที่เพิ่ม
7. รอผลลัพธ์ (ประมาณ 30-60 วินาที)
8. ไปที่แท็บ "Results" เพื่อดูข้อมูลที่ scrape ได้

### ตัวอย่างที่ 2: ตรวจสอบผลลัพธ์

1. ไปที่แท็บ "Jobs"
2. ดูสถานะการ scrape
3. ถ้า "Completed" → ไปที่แท็บ "Results"
4. ดูข้อมูลต้นไม้และราคาที่ scrape ได้
5. ตรวจสอบว่าข้อมูลถูกบันทึกลงฐานข้อมูลหรือไม่ (ไปที่หน้า "ค้นหา" หรือ "ร้านค้า")

---

## 📝 หมายเหตุ

- **AI Agent** ใช้ GPT-4o สำหรับการแกะข้อมูล
- **Scraping** ใช้ Puppeteer (ถ้ามี) หรือ Cheerio (fallback)
- **Database** จะถูกอัพเดทอัตโนมัติเมื่อ scrape สำเร็จ
- **Schedule** ยังไม่ทำงานอัตโนมัติ (ต้อง trigger เอง)

---

## 🎉 พร้อมใช้งานแล้ว!

ลองเพิ่มเว็บไซต์แรกและทดสอบ scraping ได้เลยครับ

หากมีคำถามหรือพบปัญหา แจ้งได้ครับ! 🚀

