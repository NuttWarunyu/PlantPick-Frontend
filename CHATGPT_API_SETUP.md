# 🔑 ChatGPT API Setup Guide

## ✅ การใช้ ChatGPT API ในโปรเจกต์

โปรเจกต์นี้ใช้ **OpenAI ChatGPT API** สำหรับ AI Features ดังนี้:

### 🎯 Frontend (aiService.ts)

1. **Bill Scanner** - สแกนใบเสร็จอัตโนมัติ
   - ใช้ **GPT-4o Vision** model
   - อ่านรูปใบเสร็จและแปลงเป็น JSON
   - Environment Variable: `REACT_APP_OPENAI_API_KEY`

2. **Price Analysis** - วิเคราะห์ราคา
   - ใช้ **GPT-3.5-turbo**
   - แนะนำราคาที่เหมาะสม
   - Environment Variable: `REACT_APP_OPENAI_API_KEY`

3. **Route Optimization** - วางแผนเส้นทาง
   - ใช้ **GPT-3.5-turbo**
   - แนะนำเส้นทางที่ประหยัดที่สุด
   - Environment Variable: `REACT_APP_OPENAI_API_KEY`

### 🎯 Backend (backend/services/aiService.js)

1. **Data Validation** - ตรวจสอบข้อมูลด้วย AI
   - ใช้ **GPT-3.5-turbo**
   - ตรวจสอบความถูกต้องของข้อมูลต้นไม้และร้านค้า
   - Environment Variable: `OPENAI_API_KEY`

2. **Price Analysis** - วิเคราะห์ราคาด้วย AI
   - ใช้ **GPT-3.5-turbo**
   - วิเคราะห์ว่าราคาเหมาะสมหรือไม่
   - Environment Variable: `OPENAI_API_KEY`

## 📝 วิธีตั้งค่า API Key

### 1. รับ API Key จาก OpenAI

1. ไปที่ https://platform.openai.com/api-keys
2. สร้าง API Key ใหม่
3. คัดลอก API Key (จะแสดงครั้งเดียวเท่านั้น!)

### 2. ตั้งค่าใน Vercel (Frontend)

⚠️ **ไม่ต้องตั้งค่า `REACT_APP_OPENAI_API_KEY` ใน Vercel อีกต่อไป!**

เพื่อความปลอดภัย เราเปลี่ยนให้ Frontend เรียก ChatGPT API ผ่าน Backend แทน:
- ✅ API Key จะอยู่บน Backend เท่านั้น (ไม่เปิดเผยใน Browser)
- ✅ ปลอดภัยกว่า - ไม่มี API Key ใน Frontend code
- ✅ Frontend ไม่ต้องรู้ API Key

### 3. ตั้งค่าใน Railway (Backend) - ⭐ สำคัญ!

1. ไปที่ Railway Dashboard → Project → Variables
2. เพิ่ม Environment Variable:
   - **Key**: `OPENAI_API_KEY`
   - **Value**: `sk-...` (API Key ของคุณ)
3. **Redeploy** backend service

⚠️ **สำคัญ**: API Key จะอยู่บน Backend เท่านั้น - ปลอดภัยและไม่เปิดเผยใน Browser!

### 4. ตั้งค่า Local Development

⚠️ **ไม่ต้องตั้งค่า `REACT_APP_OPENAI_API_KEY` ใน Frontend อีกต่อไป!**

สร้างไฟล์ `.env` ใน `backend/` เท่านั้น:

```env
OPENAI_API_KEY=sk-your-api-key-here
```

## 🔍 ตรวจสอบว่า API Key ทำงานหรือไม่

### Frontend (Bill Scanner)

1. ไปที่หน้า "สแกนใบเสร็จ"
2. อัพโหลดรูปใบเสร็จ
3. กด "สแกนด้วย AI"
4. ตรวจสอบ Console:
   - ถ้ามี API Key: จะเห็น API call ไปที่ OpenAI
   - ถ้าไม่มี API Key: จะใช้ Mock Data (ข้อมูลตัวอย่าง)

### Backend (AI Endpoints)

ทดสอบ API endpoint:

```bash
curl -X POST https://your-backend.railway.app/api/ai/validate \
  -H "Content-Type: application/json" \
  -d '{"data": {"name": "ทดสอบ"}, "type": "plant"}'
```

## ⚠️ หมายเหตุ

1. **API Key ต้องเก็บเป็นความลับ** - ห้าม commit ลง Git!
2. **ต้นทุนการใช้**:
   - GPT-4o Vision: ~$0.01-0.03 ต่อรูป
   - GPT-3.5-turbo: ~$0.001-0.002 ต่อ request
3. **Fallback**: ถ้าไม่มี API Key ระบบจะใช้ Mock Data
4. **Rate Limits**: OpenAI มี rate limit ตาม subscription plan

## 📊 สถานะปัจจุบัน

- ✅ Frontend AI Service: พร้อมใช้งาน (เรียกผ่าน Backend - ปลอดภัย)
- ✅ Backend AI Service: พร้อมใช้งาน (ต้องมี `OPENAI_API_KEY`)
- ✅ Bill Scanner: ใช้ GPT-4o Vision ผ่าน Backend (ปลอดภัย - API Key ไม่เปิดเผย)
- ✅ Price Analysis: ใช้ GPT-3.5-turbo ผ่าน Backend
- ✅ Route Optimization: ใช้ GPT-3.5-turbo ผ่าน Backend

## 🔒 ความปลอดภัย

✅ **แก้ไขแล้ว**: Frontend ไม่ต้องมี API Key อีกต่อไป
- Frontend ส่งรูปไป Backend
- Backend เรียก OpenAI API (ใช้ `OPENAI_API_KEY`)
- Backend ส่งผลลัพธ์กลับไป Frontend
- **API Key อยู่บน Backend เท่านั้น - ปลอดภัย!**

## 🚀 ขั้นตอนการ Deploy

### 1. **Push Code ไป GitHub**

```bash
git add .
git commit -m "feat: Move ChatGPT API calls to Backend for security"
git push origin main
```

### 2. **Railway (Backend) จะ Deploy อัตโนมัติ**

- Railway จะ auto-deploy เมื่อมี code ใหม่ใน GitHub
- รอให้ deploy เสร็จ (ประมาณ 2-3 นาที)

### 3. **Vercel (Frontend) จะ Deploy อัตโนมัติ**

- Vercel จะ auto-deploy เมื่อมี code ใหม่ใน GitHub
- รอให้ deploy เสร็จ (ประมาณ 1-2 นาที)

### 4. **ตั้งค่า `OPENAI_API_KEY` ใน Railway**

**⚠️ สำคัญ**: ต้องตั้งค่าหลังจาก deploy เสร็จแล้ว!

1. ไปที่ Railway Dashboard → Project (`lovely-rejoicing`) → Variables
2. คลิก **+ New Variable**
3. เพิ่ม:
   - **Key**: `OPENAI_API_KEY`
   - **Value**: `sk-...` (API Key จาก OpenAI)
4. คลิก **Add**
5. Railway จะ **auto-redeploy** เพื่อใช้ environment variable ใหม่

### 5. **ตรวจสอบว่า Deploy สำเร็จ**

#### Backend (Railway):
- ตรวจสอบ Logs: ควรเห็น `Plant Price API Server running on port 8080`
- ทดสอบ Health Check: `https://plantpick.app/api/health`

#### Frontend (Vercel):
- ตรวจสอบ Deployments: ควรเห็น status "Ready"
- ทดสอบ Bill Scanner: ไปที่หน้า "สแกนใบเสร็จ" และลองอัพโหลดรูป

### 6. **Test Bill Scanner**

1. ไปที่ Frontend (Vercel)
2. ไปที่หน้า "สแกนใบเสร็จ"
3. อัพโหลดรูปใบเสร็จ
4. กด "สแกนด้วย AI"
5. **ผลลัพธ์**:
   - ถ้ามี `OPENAI_API_KEY` ใน Railway → จะสแกนจริงด้วย GPT-4o Vision
   - ถ้าไม่มี API Key → จะใช้ Mock Data

### 7. **ตรวจสอบ Logs**

#### Railway Logs:
- ดูว่า backend เรียก OpenAI API ได้หรือไม่
- ดู error (ถ้ามี)

#### Browser Console:
- ดูว่า frontend เรียก `/api/ai/scan-bill` ได้หรือไม่
- ดู error (ถ้ามี)

## 🚀 ขั้นตอนถัดไป

1. ✅ เพิ่ม `OPENAI_API_KEY` ใน Railway เท่านั้น (ไม่ต้องเพิ่มใน Vercel)
3. Test Bill Scanner ด้วยรูปจริง
4. Monitor การใช้ API ใน OpenAI Dashboard

