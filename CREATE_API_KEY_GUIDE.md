# 🔑 คู่มือสร้าง Google Maps API Key

## ⚠️ ปัญหาที่พบ

หน้า Credentials แสดง **"No API keys to display"** ซึ่งหมายความว่า:
- ยังไม่ได้สร้าง API Key
- หรือ API Key ถูกลบไปแล้ว
- หรือ API Key อยู่ในโปรเจคอื่น

**URL Signing Secret** ไม่ใช่ API Key! (ใช้สำหรับ Static Maps API เท่านั้น)

---

## ✅ ขั้นตอนการสร้าง API Key

### 1. สร้าง API Key

1. ในหน้า **Credentials** ที่คุณอยู่
2. คลิกปุ่ม **"+ CREATE CREDENTIALS"** (มุมบนขวา)
3. เลือก **"API key"**
4. จะได้ API Key ใหม่ (เริ่มต้นด้วย `AIzaSy...`)
5. **คัดลอก API Key นี้ไว้** (จะแสดงแค่ครั้งเดียว!)

### 2. ตั้งค่า API Key Restrictions (แนะนำ)

เพื่อความปลอดภัย ควรจำกัด API Key:

1. คลิกที่ API Key ที่เพิ่งสร้าง
2. ในส่วน **"API restrictions"**:
   - เลือก **"Restrict key"**
   - เลือก APIs:
     - ✅ **Places API** (สำคัญที่สุด!)
     - ✅ **Geocoding API** (ถ้าใช้)
     - ✅ **Maps JavaScript API** (ถ้าใช้)
   - คลิก **"SAVE"**

3. ในส่วน **"Application restrictions"** (Optional):
   - เลือก **"IP addresses"** สำหรับ backend
   - หรือ **"HTTP referrers"** สำหรับ frontend
   - หรือ **"None"** (ชั่วคราวเพื่อทดสอบ)

### 3. เปิดใช้งาน Places API (สำคัญ!)

**ต้องเปิดใช้งาน Places API ก่อน!**

1. ไปที่ [APIs & Services → Library](https://console.cloud.google.com/apis/library)
2. ค้นหา **"Places API"**
3. คลิก **"Places API"**
4. คลิก **"ENABLE"**
5. รอสักครู่ให้ API เปิดใช้งานเสร็จ

### 4. เปิดใช้งาน Billing

1. ไปที่ [Billing](https://console.cloud.google.com/billing)
2. สร้าง Billing Account (ถ้ายังไม่มี)
3. เชื่อมโยง Billing Account กับโปรเจค
4. **ไม่ต้องกังวล**: Google ให้ $200 credit/เดือน (Free Tier)

### 5. ตั้งค่าใน Railway

1. ไปที่ [Railway Dashboard](https://railway.app/)
2. เลือกโปรเจค **PlantPick Backend**
3. ไปที่ **"Variables"** tab
4. คลิก **"New Variable"**
5. ตั้งชื่อ: `GOOGLE_MAPS_API_KEY`
6. ตั้งค่า: `your_new_api_key_here` (ใส่ API Key ที่คัดลอกมา)
7. คลิก **"Add"**
8. **Restart service** (Railway จะ restart อัตโนมัติ)

### 6. ทดสอบ

1. รอ 1-2 นาทีให้ Railway restart
2. ไปที่ `/ai-agent` → แท็บ "Google Maps"
3. คลิกปุ่ม **"ทดสอบ API Key"**
4. ควรเห็น:
   ```
   ✅ ทดสอบสำเร็จ
   Google Maps API Key ใช้งานได้!
   พบ X สถานที่
   ```

---

## 📋 Checklist

- [ ] ✅ สร้าง API Key แล้ว
- [ ] ✅ ตั้งค่า API Key Restrictions → อนุญาตให้ใช้ Places API
- [ ] ✅ เปิดใช้งาน Places API แล้ว (API enabled)
- [ ] ✅ เปิดใช้งาน Billing แล้ว
- [ ] ✅ ตั้งค่า `GOOGLE_MAPS_API_KEY` ใน Railway แล้ว
- [ ] ✅ Railway service restart แล้ว
- [ ] ✅ ทดสอบ API Key สำเร็จแล้ว

---

## 🔍 ตรวจสอบ API Key

### วิธีตรวจสอบว่า API Key ถูกต้อง:

1. ไปที่ [Credentials](https://console.cloud.google.com/apis/credentials)
2. ควรเห็น API Key ที่สร้างไว้ (มีชื่อ, วันที่สร้าง, Restrictions)
3. คลิกที่ API Key → ตรวจสอบ:
   - **API restrictions**: ต้องมี "Places API"
   - **Application restrictions**: ตั้งค่าตามต้องการ

### ทดสอบ API Key ด้วย curl:

```bash
curl "https://maps.googleapis.com/maps/api/place/textsearch/json?query=ร้านต้นไม้+กรุงเทพ&key=YOUR_API_KEY&language=th&region=th"
```

**ถ้าได้ผลลัพธ์** → API Key ใช้งานได้!  
**ถ้าได้ REQUEST_DENIED** → ตรวจสอบ Places API และ Billing

---

## ⚠️ หมายเหตุ

- **URL Signing Secret** ไม่ใช่ API Key (ใช้สำหรับ Static Maps API เท่านั้น)
- API Key จะแสดงแค่ครั้งเดียวตอนสร้าง → ต้องคัดลอกไว้!
- ถ้าลืม API Key → ต้องสร้างใหม่ (ไม่สามารถดู API Key เดิมได้)

---

**Last Updated**: 2024-12-19

