# 📖 คู่มือการลบข้อมูลต้นไม้และร้านค้า

คู่มือนี้จะอธิบายวิธีการลบข้อมูลต้นไม้และร้านค้าในระบบ PlantPick

## 🗑️ วิธีการลบข้อมูล

### 1. ลบข้อมูลทั้งหมด (Admin Only)

**⚠️ คำเตือน: การลบข้อมูลทั้งหมดจะลบทุกอย่างรวมถึง:**
- ต้นไม้ทั้งหมด
- ร้านค้าทั้งหมด
- ความสัมพันธ์ plant-supplier
- ใบเสร็จทั้งหมด

**วิธีใช้:**

#### ผ่านหน้าเว็บ (Database Management Page)
1. เข้าสู่ระบบเป็น Admin
2. ไปที่หน้า **"จัดการฐานข้อมูล"** (Database Management)
3. คลิกปุ่ม **"🗑️ ลบข้อมูลทั้งหมด (ต้นไม้ + ร้านค้า)"**
4. ยืนยันการลบ 2 ครั้ง:
   - คลิก OK ใน dialog แรก
   - พิมพ์ **"ลบทั้งหมด"** ใน prompt

#### ผ่าน API
```bash
curl -X DELETE https://your-api-url/api/admin/data/clear-all \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_ADMIN_TOKEN" \
  -H "x-admin-token: YOUR_ADMIN_TOKEN" \
  -d '{"confirm": "DELETE_ALL_DATA"}'
```

**Response:**
```json
{
  "success": true,
  "message": "ลบข้อมูลทั้งหมดสำเร็จแล้ว (plants, suppliers, plant_suppliers, bills)",
  "data": {
    "deleted": {
      "plants": "all",
      "suppliers": "all",
      "plantSuppliers": "all",
      "bills": "all"
    }
  }
}
```

---

### 2. ลบต้นไม้รายการเดียว

**วิธีใช้:**

#### ผ่าน API
```bash
curl -X DELETE https://your-api-url/api/plants/PLANT_ID
```

**ตัวอย่าง:**
```bash
curl -X DELETE http://localhost:3002/api/plants/plant_12345
```

**Response:**
```json
{
  "success": true,
  "data": {
    "id": "plant_12345",
    "name": "ต้นไม้ที่ถูกลบ",
    ...
  },
  "message": "ลบข้อมูลต้นไม้สำเร็จ"
}
```

**หมายเหตุ:** การลบต้นไม้จะลบความสัมพันธ์ plant-supplier ที่เกี่ยวข้องด้วยอัตโนมัติ

---

### 3. ลบร้านค้ารายการเดียว

**วิธีใช้:**

#### ผ่าน API
```bash
curl -X DELETE https://your-api-url/api/suppliers/SUPPLIER_ID
```

**ตัวอย่าง:**
```bash
curl -X DELETE http://localhost:3002/api/suppliers/supplier_67890
```

**Response:**
```json
{
  "success": true,
  "data": {
    "id": "supplier_67890",
    "name": "ร้านค้าที่ถูกลบ",
    ...
  },
  "message": "ลบข้อมูลร้านค้าสำเร็จ"
}
```

**หมายเหตุ:** การลบร้านค้าจะลบความสัมพันธ์ plant-supplier ที่เกี่ยวข้องด้วยอัตโนมัติ

---

### 4. ลบร้านค้าจากต้นไม้เฉพาะ (Plant-Supplier Relationship)

**วิธีใช้:**

#### ผ่าน API
```bash
curl -X DELETE https://your-api-url/api/plants/PLANT_ID/suppliers/SUPPLIER_ID
```

**ตัวอย่าง:**
```bash
curl -X DELETE http://localhost:3002/api/plants/plant_12345/suppliers/supplier_67890
```

**Response:**
```json
{
  "success": true,
  "data": {
    "id": "supplier_67890",
    ...
  },
  "message": "ลบข้อมูลผู้จัดจำหน่ายสำเร็จ"
}
```

**หมายเหตุ:** Endpoint นี้ใช้สำหรับลบความสัมพันธ์ระหว่างต้นไม้และร้านค้าเท่านั้น ไม่ได้ลบร้านค้าจากระบบ

---

## 🔍 ตรวจสอบข้อมูลก่อนลบ

### ดูข้อมูลต้นไม้ทั้งหมด
```bash
curl https://your-api-url/api/plants
```

### ดูข้อมูลร้านค้าทั้งหมด
```bash
curl https://your-api-url/api/suppliers
```

### ดูข้อมูลต้นไม้เฉพาะ
```bash
curl https://your-api-url/api/plants/PLANT_ID
```

---

## ⚠️ คำเตือนสำคัญ

1. **การลบข้อมูลไม่สามารถย้อนกลับได้** - กรุณาสร้าง Backup ก่อนลบข้อมูลสำคัญ
2. **Foreign Key Constraints** - ระบบจะลบข้อมูลที่เกี่ยวข้องอัตโนมัติ (plant_suppliers, bills, etc.)
3. **Admin Authentication** - การลบข้อมูลทั้งหมดต้องใช้ Admin Token
4. **การยืนยัน** - การลบข้อมูลทั้งหมดต้องยืนยัน 2 ครั้งเพื่อความปลอดภัย

---

## 💾 สร้าง Backup ก่อนลบ

### ผ่านหน้าเว็บ
1. ไปที่หน้า **"จัดการฐานข้อมูล"**
2. คลิกปุ่ม **"สร้าง Backup"** หรือ **"Export"**
3. บันทึกไฟล์ JSON ที่ดาวน์โหลดมา

### ผ่าน API
```bash
curl https://your-api-url/api/plants > plants_backup.json
curl https://your-api-url/api/suppliers > suppliers_backup.json
```

---

## 📝 ตัวอย่างการใช้งานใน JavaScript/TypeScript

### ลบต้นไม้
```typescript
const deletePlant = async (plantId: string) => {
  const response = await fetch(`/api/plants/${plantId}`, {
    method: 'DELETE',
  });
  const data = await response.json();
  return data;
};
```

### ลบร้านค้า
```typescript
const deleteSupplier = async (supplierId: string) => {
  const response = await fetch(`/api/suppliers/${supplierId}`, {
    method: 'DELETE',
  });
  const data = await response.json();
  return data;
};
```

### ลบข้อมูลทั้งหมด (Admin)
```typescript
const deleteAllData = async (adminToken: string) => {
  const response = await fetch('/api/admin/data/clear-all', {
    method: 'DELETE',
    headers: {
      'Content-Type': 'application/json',
      'Authorization': `Bearer ${adminToken}`,
      'x-admin-token': adminToken,
    },
    body: JSON.stringify({ confirm: 'DELETE_ALL_DATA' }),
  });
  const data = await response.json();
  return data;
};
```

---

## 🆘 หากเกิดข้อผิดพลาด

### Error: "ไม่พบข้อมูลต้นไม้/ร้านค้า"
- ตรวจสอบว่า ID ถูกต้อง
- ตรวจสอบว่าข้อมูลยังมีอยู่ในระบบ

### Error: "Foreign key constraint violation"
- ระบบจะจัดการอัตโนมัติ แต่ถ้ายังมีปัญหา ให้ตรวจสอบว่าไม่มีข้อมูลอื่นอ้างอิงอยู่

### Error: "Unauthorized" (สำหรับลบข้อมูลทั้งหมด)
- ตรวจสอบว่าใช้ Admin Token ที่ถูกต้อง
- ตรวจสอบว่า Token ยังไม่หมดอายุ

---

## 📞 ติดต่อสอบถาม

หากมีปัญหาหรือคำถามเพิ่มเติม กรุณาติดต่อทีมพัฒนา

