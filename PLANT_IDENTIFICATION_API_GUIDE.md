# 🌿 Plant Identification API Guide

## คำถาม: ต้องการระบุต้นไม้ละเอียดขึ้น เช่น ชื่อต้นไม้ใหญ่จริงๆ ไม้พุ่มจริงๆ

## 🎯 API ที่แนะนำสำหรับการระบุพืชพันธุ์

### 1. **PlantNet API** (แนะนำมากที่สุด) ⭐

**ข้อดี:**
- ✅ ฟรี (มี rate limit)
- ✅ ระบุพืชพันธุ์ได้แม่นยำมาก
- ✅ มีชื่อวิทยาศาสตร์และชื่อสามัญ
- ✅ รองรับหลายภาษา (รวมภาษาไทย)
- ✅ มีฐานข้อมูลพืชที่ครอบคลุม
- ✅ API ใช้งานง่าย

**ข้อเสีย:**
- ⚠️ Rate limit: 500 requests/day (free tier)
- ⚠️ ต้องใช้ API key

**Website:** https://my.plantnet.org/
**API Docs:** https://my.plantnet.org/api/doc

**ตัวอย่างการใช้งาน:**
```javascript
const response = await fetch('https://my-api.plantnet.org/v2/identify/{project}', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
  },
  body: JSON.stringify({
    images: [base64Image],
    modifiers: ['crops_fast', 'similar_images'],
    plant_details: ['common_names', 'url', 'name_authority', 'wiki_description', 'synonyms']
  })
});
```

---

### 2. **Plant.id API**

**ข้อดี:**
- ✅ ระบุพืชพันธุ์ได้แม่นยำ
- ✅ มีชื่อวิทยาศาสตร์
- ✅ รองรับหลายภาษา

**ข้อเสีย:**
- ⚠️ ต้องจ่ายเงิน (มี free tier แต่จำกัด)
- ⚠️ Rate limit ต่ำ

**Website:** https://plant.id/

---

### 3. **iNaturalist API**

**ข้อดี:**
- ✅ ฟรี
- ✅ ฐานข้อมูลใหญ่
- ✅ Community-driven (มีคนช่วยยืนยัน)

**ข้อเสีย:**
- ⚠️ API ซับซ้อนกว่า
- ⚠️ อาจไม่แม่นยำเท่า PlantNet

**Website:** https://www.inaturalist.org/
**API Docs:** https://api.inaturalist.org/v1/docs/

---

### 4. **Google Cloud Vision API** (Plant Detection)

**ข้อดี:**
- ✅ ใช้ Google Cloud ที่มีอยู่แล้ว
- ✅ ระบุพืชพันธุ์ได้ดี

**ข้อเสีย:**
- ⚠️ ต้องจ่ายเงิน
- ⚠️ ไม่เฉพาะเจาะจงเท่า PlantNet

---

## 💡 แนะนำ: ใช้ PlantNet API

### เหตุผล:
1. **แม่นยำที่สุด** - ถูกออกแบบมาเฉพาะสำหรับการระบุพืชพันธุ์
2. **ฟรี** - มี free tier ที่เพียงพอสำหรับการใช้งานทั่วไป
3. **ใช้งานง่าย** - API ไม่ซับซ้อน
4. **รองรับภาษาไทย** - มีชื่อพืชภาษาไทยในฐานข้อมูล

### การ Integration:

#### Option 1: ใช้ PlantNet เป็น Primary + GPT-4o เป็น Fallback
- ใช้ PlantNet สำหรับระบุต้นไม้แต่ละต้น (แม่นยำ)
- ใช้ GPT-4o สำหรับวิเคราะห์ภาพรวม (องค์ประกอบอื่นๆ)

#### Option 2: ใช้ PlantNet สำหรับต้นไม้ที่ GPT-4o ไม่แน่ใจ
- GPT-4o วิเคราะห์ก่อน
- ถ้าไม่แน่ใจชื่อต้นไม้ → ส่งไป PlantNet เพื่อระบุ

#### Option 3: ใช้ PlantNet แทน GPT-4o สำหรับต้นไม้
- ใช้ PlantNet สำหรับระบุต้นไม้ทั้งหมด
- ใช้ GPT-4o สำหรับองค์ประกอบอื่นๆ (สนามหญ้า, ทางเดิน)

---

## 📝 ขั้นตอนการ Setup PlantNet API

1. **สมัคร Account:**
   - ไปที่ https://my.plantnet.org/
   - สร้าง account
   - สร้าง Project (เลือก "World" หรือ "Asia")

2. **รับ API Key:**
   - ไปที่ Project Settings
   - Copy API Key

3. **เพิ่มใน Backend:**
   ```bash
   # Railway Variables
   PLANTNET_API_KEY=your_api_key_here
   ```

4. **สร้าง Service:**
   ```javascript
   // backend/services/plantNetService.js
   async identifyPlant(base64Image) {
     const response = await fetch('https://my-api.plantnet.org/v2/identify/world', {
       method: 'POST',
       headers: {
         'Content-Type': 'application/json',
       },
       body: JSON.stringify({
         images: [base64Image],
         modifiers: ['crops_fast', 'similar_images'],
         plant_details: ['common_names', 'name_authority', 'synonyms']
       })
     });
     return response.json();
   }
   ```

---

## 🎯 สรุป

**แนะนำใช้ PlantNet API** เพราะ:
- ✅ แม่นยำที่สุดสำหรับการระบุพืชพันธุ์
- ✅ ฟรี (มี rate limit)
- ✅ ใช้งานง่าย
- ✅ รองรับภาษาไทย

**การใช้งาน:**
- ใช้ PlantNet สำหรับระบุต้นไม้ (แม่นยำ)
- ใช้ GPT-4o สำหรับวิเคราะห์องค์ประกอบอื่นๆ (สนามหญ้า, ทางเดิน, ฯลฯ)

