# 📊 หน้า "จัดการโปรเจกต์" - ข้อมูลเก็บที่ไหน?

## 🔍 สถานะปัจจุบัน:

### ❌ ปัญหา:
- **เก็บข้อมูลแค่ใน React State** (memory เท่านั้น)
- **ไม่บันทึกไป database**
- **ไม่บันทึกไป localStorage**
- **Refresh หน้าเว็บ → ข้อมูลหายหมด!** (กลับไปเป็น mock data)
- **ถ้ามีคนอื่นใช้ → ไม่เห็นโปรเจกต์ของกัน** (เพราะไม่เก็บใน database)

---

## 📝 โค้ดปัจจุบัน:

```typescript
// ProjectPage.tsx - line 33-78
const loadProjects = async () => {
  // โหลด mock data (hardcoded)
  const mockProjects: Project[] = [...];
  setProjects(mockProjects); // เก็บแค่ใน React state
};

const handleCreateProject = async () => {
  // เพิ่มโปรเจกต์ใน state เท่านั้น
  setProjects([project, ...projects]); // ไม่บันทึกไปไหน!
};
```

**ผลลัพธ์:**
- ❌ ข้อมูลไม่ persist (refresh หน้า → หาย)
- ❌ หลายคนใช้ → ไม่เห็นโปรเจกต์ของกัน
- ❌ ข้อมูลแค่ใน memory ของ browser

---

## 🎯 สิ่งที่ต้องทำ:

### ทางเลือกที่ 1: เก็บใน Database (แนะนำ - สำหรับหลายคนใช้)

**ข้อดี:**
- ✅ ทุกคนเห็นข้อมูลเดียวกัน
- ✅ ข้อมูล persist (ไม่หายเมื่อ refresh)
- ✅ ใช้งานได้จากหลายอุปกรณ์

**ข้อเสีย:**
- ❌ **ถ้าไม่มี login → ทุกคนเห็นข้อมูลเดียวกันทั้งหมด** (ไม่แยกคน)
- ❌ คนอื่นอาจแก้ไข/ลบโปรเจกต์ของคุณได้

**วิธีแก้:**
- ต้องทำระบบ login ก่อน
- หรือเก็บ `user_id` ในโปรเจกต์ (แต่ถ้าไม่มี login → ไม่มี user_id)

---

### ทางเลือกที่ 2: เก็บใน localStorage (ชั่วคราว - สำหรับใช้คนเดียว)

**ข้อดี:**
- ✅ ข้อมูล persist (ไม่หายเมื่อ refresh)
- ✅ แต่ละคนมีข้อมูลแยกกัน (แต่ละ browser)
- ✅ ไม่ต้องทำ login

**ข้อเสีย:**
- ❌ ข้อมูลแยกตาม browser (คนละเครื่อง → คนละข้อมูล)
- ❌ ใช้ไม่ได้จากหลายอุปกรณ์ (ข้อมูลไม่ sync)
- ❌ ถ้า clear browser data → ข้อมูลหาย

---

### ทางเลือกที่ 3: เก็บใน Database + localStorage (Hybrid)

**ข้อดี:**
- ✅ ข้อมูล sync จาก database
- ✅ ทำงาน offline ได้ (ใช้ localStorage)
- ✅ หลายคนเห็นข้อมูลเดียวกัน

**ข้อเสีย:**
- ❌ **ถ้าไม่มี login → ทุกคนเห็นข้อมูลเดียวกันทั้งหมด**
- ❌ ซับซ้อนกว่า

---

## 📊 สรุป:

### สถานะปัจจุบัน:
```
ข้อมูลโปรเจกต์ → React State (memory) → Refresh หน้า → หายหมด ❌
```

### ทางเลือก:

#### 1. **Database (ไม่มี login):**
```
ทุกคน → Database เดียวกัน → เห็นโปรเจกต์เดียวกันทั้งหมด
❌ ไม่แยกคน
❌ คนอื่นอาจแก้ไข/ลบได้
```

#### 2. **localStorage (ไม่มี login):**
```
แต่ละ browser → localStorage แยกกัน → ข้อมูลแยกคน
✅ แต่ละคนมีข้อมูลของตัวเอง
❌ ข้อมูลไม่ sync ระหว่างอุปกรณ์
```

---

## 💡 คำแนะนำ:

### ถ้ายังไม่มีระบบ Login:

**แนะนำใช้ localStorage ชั่วคราว:**
- ✅ แต่ละคนมีข้อมูลแยกกัน (แต่ละ browser)
- ✅ ข้อมูล persist (ไม่หายเมื่อ refresh)
- ✅ ทำง่าย (ไม่ต้องทำ login)

**โค้ดที่จะเพิ่ม:**
```typescript
// เก็บใน localStorage
localStorage.setItem('projects', JSON.stringify(projects));

// โหลดจาก localStorage
const stored = localStorage.getItem('projects');
if (stored) {
  setProjects(JSON.parse(stored));
}
```

---

### ถ้าต้องการหลายคนใช้ร่วมกัน:

**ต้องทำระบบ Login ก่อน:**
- เพิ่ม User system
- เก็บ `user_id` ในแต่ละโปรเจกต์
- Filter โปรเจกต์ตาม `user_id`
- หรือใช้ Shared Projects (ทุกคนเห็นร่วมกัน)

---

## ❓ ต้องการให้แก้ไขเป็นแบบไหน?

1. **เก็บใน localStorage** → แต่ละคนมีข้อมูลแยกกัน (แต่ละ browser)
2. **เก็บใน Database** → ทุกคนเห็นข้อมูลเดียวกัน (ไม่มี login)
3. **รอทำ Login ก่อน** → แล้วค่อยเก็บใน Database (แยกตาม user)

บอกได้เลยครับว่าต้องการแบบไหน ผมจะช่วยแก้ไขให้! 😊

