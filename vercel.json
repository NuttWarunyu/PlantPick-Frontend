{
  "buildCommand": "cd plant-price-system && npm install && npm run build",
  "outputDirectory": "plant-price-system/build",
  "installCommand": "cd plant-price-system && npm install",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
